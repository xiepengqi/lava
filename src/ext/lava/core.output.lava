(def -out (. java.lang.System out))

(/println 
	'same like System.out.println'
	(as java.lang.Object $0)
	(if (def? $0)
	    (.println -out $0)))
	

(/print 
	'same like System.out.print'
	(as java.lang.Object $0)
	(if (def? $0)
	    (.print -out $0)))
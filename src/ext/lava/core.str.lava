(use core.bean)
(/join
    (def sb (newStringBuilder))
    (def joinStr
         (if (def? $1)
              $1
              ""))
    (foreach $args /append)
    (/append
        (if (> (.size sb) 0)
            (.append sb joinStr))
        (.append sb $0))
    (.toString sb))


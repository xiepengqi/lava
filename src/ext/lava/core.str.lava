(use core.bean)
(/join
    (def sb (newStrBuilder))
    (def joinStr
         (if (def? $1)
              $1
              ""))
    (foreach $args /append)
    (/append
        (if (> (.length sb) 0)
            (.append sb joinStr))
        (.append sb $0))
    (.toString sb))

